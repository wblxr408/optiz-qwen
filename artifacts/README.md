# Artifacts

Store generated non-source artifacts here, for example:

- converted checkpoints
- quantized weights
- profiling dumps
- temporary deployment packages

This directory should remain empty in git except for placeholder files.

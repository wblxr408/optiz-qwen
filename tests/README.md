# Tests

Planned coverage:

- config loading
- dataset adapter validation
- metric correctness
- optimization toggle routing
- regression checks for baseline vs optimized paths

Real tests should be added only when the corresponding modules are implemented.

# Benchmarks

Store benchmark records here.

Recommended outputs:

- baseline metrics
- optimized metrics
- ablation tables
- raw timing logs

Every saved result should indicate:

- config name
- hardware
- commit or version note
- dataset slice
